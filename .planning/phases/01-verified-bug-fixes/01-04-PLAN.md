---
phase: 01-verified-bug-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - easycord/event_bus.py
  - easycord/_bot_plugins.py
  - tests/test_event_bus.py
  - tests/test_hot_reload.py
autonomous: true
requirements: [REQ-04]
must_haves:
  truths:
    - "When an EventBus subscriber raises, the logged error includes the failing handler's __qualname__ (or repr fallback)"
    - "Context-menu removal failures during plugin unload emit a logger.debug entry instead of a silent pass"
    - "EventBus listener invocation order (registration order) is documented in the publish/subscribe docstrings"
  artifacts:
    - tests/test_event_bus.py
    - tests/test_hot_reload.py
  key_links:
    - "publish() gather results -> (callback, coro) pairing -> handler __qualname__ in error log"
    - "_bot_plugins.py:156-163 context-menu removal except -> logger.debug matching slash branch at 142-146"
---

<objective>
Close the observability gaps (per REQ-04): EventBus subscriber-failure logs must name the failing handler (`__qualname__`), plugin-unload context-menu removals must debug-log instead of silently swallowing exceptions, and EventBus listener invocation order must be documented.

Purpose: `event_bus.py` publish logs the event name but not which subscriber failed, and `_bot_plugins.py:156-163` has two `except Exception: pass` blocks that hide context-menu removal failures during unload (unlike the slash branch at 142-146 which debug-logs). Undocumented listener order makes ordering-dependent bugs hard to reason about.
Output: modified `easycord/event_bus.py` + `easycord/_bot_plugins.py` + extended `tests/test_event_bus.py` and `tests/test_hot_reload.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-bug-fixes/01-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01-04)

- Modified `easycord/event_bus.py`: `publish()` error logs include handler identity `getattr(callback, "__qualname__", repr(callback))`; async-subscriber gather results mapped back to their handler by keeping `(callback, coro)` pairs; listener-order note added to `publish`/`subscribe` docstrings
- Modified `easycord/_bot_plugins.py`: the two `except Exception: pass` context-menu removals at lines 156-163 replaced with `logger.debug(...)` matching the slash branch (142-146)
- Extended `tests/test_event_bus.py` and `tests/test_hot_reload.py`
- No new public symbols

## Interface context for executors

- `event_bus.py:53-80` `publish`: sync callbacks are called inline (except-logged at 73-74 without identity); async callbacks are collected into `tasks = []` (losing the callback reference) then `results = await asyncio.gather(*tasks, return_exceptions=True)` and each exception is logged at 78-80 WITHOUT the handler name. To name the async handler, keep pairs — e.g. build `pending: list[tuple[Callable, Awaitable]]` and gather over `[coro for _, coro in pending]`, then zip results back to their callback. `_listeners` is a `dict[str, list[Callable]]` appended in `subscribe` (line 30) — invocation order is registration order.
- `_bot_plugins.py:142-146` is the reference debug-log pattern for the slash branch (`logger.debug("Could not remove command %r during unload", cmd_name)`). Lines 152-163 handle user- and message-command removal with bare `except Exception: pass` — replace both with an equivalent `logger.debug(...)` that names the context-menu command and its type.

<tasks>

<task type="auto">
  <name>Task 1: Verify the finding — missing handler identity in EventBus logs and silent context-menu unload passes</name>
  <files>easycord/event_bus.py, easycord/_bot_plugins.py</files>
  <read_first>
    - easycord/event_bus.py (lines 13-80: subscribe, unsubscribe, publish)
    - easycord/_bot_plugins.py (lines 130-175: the unload loop, slash branch 142-146, context-menu branches 152-163)
    - tests/test_event_bus.py (existing coverage, caplog usage if any)
    - tests/test_hot_reload.py (existing unload coverage)
  </read_first>
  <action>
    Per the locked verify-first rule, confirm before changing anything: (a) in `event_bus.py` the sync-subscriber log at 73-74 and the async-subscriber log at 78-80 do NOT include the failing handler's `__qualname__`/identity, and the async path discards the callback reference by appending bare coroutines to `tasks`; (b) `_bot_plugins.py:156-163` has two `except Exception: pass` blocks for user/message context-menu removal with no debug log, unlike the slash branch at 142-146. If either is already fixed, degrade that item to regression-test-only.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && grep -n "__qualname__\|gather\|except Exception" easycord/event_bus.py; echo "---"; grep -n "pass\|logger.debug" easycord/_bot_plugins.py | sed -n '1,40p'</automated>
  </verify>
  <acceptance_criteria>
    - Recorded: async publish path currently loses the callback reference (no identity in log).
    - Recorded: the two context-menu removal branches currently use `except Exception: pass`.
  </acceptance_criteria>
  <done>Both observability sub-findings classified open vs already-fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests, then add handler identity, unload debug logs, and order docstrings</name>
  <files>tests/test_event_bus.py, tests/test_hot_reload.py, easycord/event_bus.py, easycord/_bot_plugins.py</files>
  <read_first>
    - easycord/event_bus.py (publish 53-80)
    - easycord/_bot_plugins.py (unload loop 130-175, slash branch 142-146)
    - tests/test_event_bus.py (extend)
    - tests/test_hot_reload.py (extend)
  </read_first>
  <behavior>
    - When a subscribed async handler with a known `__qualname__` raises during `publish`, the captured error log record contains that handler's `__qualname__`. Use `caplog` to assert the qualname substring appears in the EventBus error log.
    - When a subscribed SYNC handler raises during `publish`, its identity likewise appears in the error log.
    - Handlers fire in registration order (assert a shared list records call order matching subscribe order) — this both documents and guards the ordering contract.
    - During plugin unload, when `tree.remove_command` for a user/message context menu raises, a `logger.debug` record is emitted (assert via `caplog` at DEBUG level) rather than silent swallowing; unload still completes without raising.
  </behavior>
  <action>
    Extend `tests/test_event_bus.py` and `tests/test_hot_reload.py` with the four behaviors FIRST and confirm they FAIL. Then implement: in `event_bus.py publish`, log the failing handler identity via `getattr(callback, "__qualname__", repr(callback))` for BOTH the sync except path (73-74) and the async path — restructure the async collection to keep `(callback, coro)` pairs so each gather result (78-80) maps back to its handler and is logged with that handler's identity. Add a short note to the `publish` and `subscribe` docstrings stating that listeners are invoked in registration order. In `_bot_plugins.py`, replace the two `except Exception: pass` context-menu removal blocks (156-163) with `logger.debug(...)` calls that name the context-menu command and its app-command type, mirroring the slash branch at 142-146. Re-run and confirm green. Use `caplog`/`asyncio.Event`-style synchronization — no wall-clock assertions.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && python -m pytest tests/test_event_bus.py tests/test_hot_reload.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_event_bus.py tests/test_hot_reload.py -q` passes.
    - A test asserts a failing async subscriber's `__qualname__` appears in the EventBus error log; another asserts the same for a sync subscriber.
    - `grep -c "__qualname__" easycord/event_bus.py` is >= 1.
    - `grep -c "except Exception:" easycord/_bot_plugins.py` for the context-menu branches is reduced and each is followed by a `logger.debug` (no bare `pass` remains on those two branches); a test asserts a debug record is emitted on context-menu removal failure during unload.
  </acceptance_criteria>
  <done>EventBus failures name their handler, context-menu unload failures are debug-logged, and listener order is documented — all proven with caplog-based regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin -> EventBus | Third-party plugin handlers may raise; failures must be observable, not silent |
| Plugin unload -> command tree | Removal failures must be logged for diagnosability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Repudiation | EventBus subscriber failures logged without handler identity | low | mitigate | Include `__qualname__` in error logs so a failing handler is attributable and debuggable |
| T-01-07 | Repudiation | Silent `except: pass` on context-menu unload hiding failures | low | mitigate | Emit `logger.debug` on removal failure so unload issues are diagnosable |
</threat_model>

<verification>
Run: `python -m pytest tests/test_event_bus.py tests/test_hot_reload.py -q` — green.
Ownership check: this plan touches only `easycord/event_bus.py`, `easycord/_bot_plugins.py`, and the two test files. It must NOT touch bot.py (owned by 01-03).
</verification>

<success_criteria>
- EventBus subscriber-failure logs name the failing handler.
- Context-menu unload failures debug-log instead of silent pass.
- Listener invocation order documented in docstrings.
</success_criteria>

<output>
Create `.planning/phases/01-verified-bug-fixes/01-04-SUMMARY.md` when done.
</output>
