---
phase: 01-verified-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - easycord/cli.py
  - tests/test_cli_scaffold.py
autonomous: true
requirements: [REQ-02]
must_haves:
  truths:
    - "A scaffold generated from a project name that maps to a pytest-colliding module (test_*/*_test) produces a plugin module file that pytest does not collect"
    - "The generated pyproject.toml [tool.pytest.ini_options] table contains testpaths set to the tests directory"
    - "The user is warned when the requested name is renamed to avoid a test_* collision"
  artifacts:
    - tests/test_cli_scaffold.py
  key_links:
    - "_module_name() rename rule -> generated plugins/<module>.py filename -> pytest collection scope"
    - "_base_project_files pyproject template -> [tool.pytest.ini_options].testpaths"
---

<objective>
Fix the CLI scaffold pytest collision (per REQ-02): generated projects must not produce a plugin module named `test_*`/`*_test` (which pytest would collect as a test file), and the generated `pyproject.toml` must scope pytest to the tests directory via a `testpaths` entry.

Purpose: `easycord/cli.py` `_module_name` currently slugs the project name without guarding against pytest-colliding names, and the generated pyproject's `[tool.pytest.ini_options]` lacks a `testpaths` key, so a scaffolded plugin module can be collected by pytest.
Output: modified `easycord/cli.py` + new `tests/test_cli_scaffold.py`. Existing `tests/test_plugin_creator.py` stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-bug-fixes/01-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01-02)

- New file: `tests/test_cli_scaffold.py`
- Modified `easycord/cli.py`: `_module_name()` gains a `test_*`/`*_test` rename+warn rule (e.g. `bot_` prefix); `_base_project_files()` pyproject template gains a `testpaths` entry scoping pytest to the tests dir
- New CLI behavior: a warning emitted when a colliding name is renamed
- No new public symbols

## Interface context for executors

- `_module_name(name: str) -> str` at `easycord/cli.py:54-60` currently returns a lowercased/underscored slug and only guards the leading-digit case (`plugin_<n>`). The generated plugin file is `f"plugins/{plugin_module}.py"` (`cli.py:160`).
- `_base_project_files(name)` at `cli.py:88-104` emits the pyproject with `[tool.pytest.ini_options]` containing only `asyncio_mode = "auto"`. The template is an indented triple-quoted string that is dedented before write — preserve the existing dedent behavior so the added `testpaths` line aligns.
- Scaffold write path lives in `easycord/plugin_creator.py` (`create_plugin_scaffold`). Read `tests/test_plugin_creator.py` to see how a scaffold is generated into `tmp_path` and asserted, and mirror that harness for the new test.

<tasks>

<task type="auto">
  <name>Task 1: Verify the finding — confirm test_* collision and missing testpaths in generated scaffold</name>
  <files>easycord/cli.py</files>
  <read_first>
    - easycord/cli.py (lines 46-178: `_class_name`, `_module_name`, `_base_project_files`, `_minimal_project_files`, `_plugin_project_files`)
    - easycord/plugin_creator.py (the write-to-disk path used by `easycord new`)
    - tests/test_plugin_creator.py (how existing tests drive scaffold generation into tmp_path)
  </read_first>
  <action>
    Per the locked verify-first rule, confirm the finding before changing anything. Confirm that (a) `_module_name` returns e.g. `test_bot` unchanged for a project name that slugs to `test_*` (no rename guard), so the generated `plugins/<module>.py` would be a pytest-collectable name; and (b) the pyproject emitted by `_base_project_files` has a `[tool.pytest.ini_options]` table with no `testpaths` key. Record the exact indentation/dedent handling of the pyproject template string so the added `testpaths` line lands correctly after dedent. If EITHER sub-fix is already present on disk, degrade that item to regression-test-only.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && grep -n "testpaths" easycord/cli.py; echo "testpaths-grep-done"; grep -n "def _module_name" easycord/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - Confirmed whether `testpaths` already exists in `cli.py` (grep result recorded).
    - Confirmed whether `_module_name` already rejects/renames `test_*`/`*_test` (recorded).
    - The pyproject template's dedent/indent handling is documented so the new line renders aligned.
  </acceptance_criteria>
  <done>Baseline established: which of the two sub-fixes are genuinely open vs already present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing scaffold tests, then add testpaths + test_* rename-with-warning</name>
  <files>tests/test_cli_scaffold.py, easycord/cli.py</files>
  <read_first>
    - easycord/cli.py (the three project-file generators + `_module_name`)
    - easycord/plugin_creator.py (scaffold write path)
    - tests/test_plugin_creator.py (scaffold-to-tmp_path assertion pattern to reuse)
  </read_first>
  <behavior>
    - Generating a scaffold with a project name that slugs to a `test_*`/`*_test` module (e.g. name "test bot") produces a plugin module file whose stem does NOT match pytest's `test_*`/`*_test` collection pattern (e.g. a `bot_` prefix is applied), and a warning is surfaced reporting the rename.
    - Generating any scaffold produces a `pyproject.toml` whose `[tool.pytest.ini_options]` table contains a `testpaths` entry pointing at the tests dir (and still `asyncio_mode = "auto"`).
    - A normal (non-colliding) project name is unaffected — `_module_name("my cool bot")` still yields `my_cool_bot`.
    - Collecting the generated project's `plugins/` module yields zero pytest test items (assert via `_module_name` result not matching `test_*`/`*_test`, or a `pytest --collect-only` over the generated dir).
  </behavior>
  <action>
    Create `tests/test_cli_scaffold.py`. Write tests FIRST asserting the four behaviors above by generating scaffolds into `tmp_path` (mirror `tests/test_plugin_creator.py`), then run and confirm they FAIL. Then edit `easycord/cli.py`: in `_module_name`, after computing `lowered`, detect names matching `test_*` or `*_test` and rename by applying a clear prefix (e.g. `bot_`) so the result no longer matches pytest collection, emitting a warning that names the original and the renamed module. In `_base_project_files`, add a `testpaths` entry pointing at the tests dir inside the `[tool.pytest.ini_options]` table, respecting the dedent handling recorded in Task 1. Re-run and confirm green. Keep `tests/test_plugin_creator.py` green (do not change its expected non-colliding outputs). No wall-clock assertions.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && python -m pytest tests/test_cli_scaffold.py tests/test_plugin_creator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_cli_scaffold.py` exists and passes.
    - `grep -c "testpaths" easycord/cli.py` is >= 1.
    - A test asserts `_module_name` renames a `test_*` name to a non-collecting module and that a warning is emitted.
    - `python -m pytest tests/test_plugin_creator.py -q` stays green.
  </acceptance_criteria>
  <done>Generated scaffolds no longer collide with pytest collection and scope pytest to the tests dir; proven by the new regression file with the plugin-creator suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer -> `easycord new` scaffold | User-supplied project name flows into generated file names and pyproject content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Scaffold plugin module masquerading as a test file | low | mitigate | Rename `test_*`/`*_test` module names and scope pytest via `testpaths` so generated plugin code is never collected/executed as tests |
</threat_model>

<verification>
Run: `python -m pytest tests/test_cli_scaffold.py tests/test_plugin_creator.py -q` — green.
Ownership check: this plan touches only `easycord/cli.py` and the new test file.
</verification>

<success_criteria>
- Generated scaffolds cannot produce a pytest-collectable plugin module.
- Generated pyproject scopes pytest to the tests dir via `testpaths`.
- A rename warning is surfaced; plugin-creator suite stays green.
</success_criteria>

<output>
Create `.planning/phases/01-verified-bug-fixes/01-02-SUMMARY.md` when done.
</output>
