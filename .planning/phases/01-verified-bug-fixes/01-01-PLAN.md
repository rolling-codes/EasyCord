---
phase: 01-verified-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - easycord/plugins/tickets.py
  - easycord/plugins/reaction_roles.py
  - easycord/plugins/verification.py
  - easycord/plugins/word_filter.py
  - easycord/plugins/welcome.py
  - easycord/plugins/giveaway.py
  - easycord/plugins/polls.py
  - easycord/plugins/starboard.py
  - easycord/plugins/member_logging.py
  - tests/test_bot_permissions_adoption.py
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - "Privileged commands in tickets, reaction_roles, verification, word_filter, welcome, giveaway, polls, starboard, member_logging declare bot_permissions on their @slash decorator"
    - "When the bot lacks a declared permission, the privileged command responds with the localized errors.bot_permissions_missing message and does not execute its privileged Discord call"
    - "When the bot has all declared permissions, the command executes normally"
  artifacts:
    - tests/test_bot_permissions_adoption.py
  key_links:
    - "@slash(bot_permissions=[...]) -> build_slash_callback bot_permissions branch (_command_callbacks.py:179-206) -> ctx.bot_permissions -> localized denial"
---

<objective>
Adopt the existing `bot_permissions` mechanism (per REQ-01) across 9 privileged built-in plugins so each command that calls a privileged Discord API fails fast with a localized message when the bot lacks the needed permission, instead of executing partway and raising `Forbidden`.

Purpose: Only `plugins/moderation.py` currently uses `bot_permissions`; the mechanism (`_command_callbacks.py:179-206`) is fully built but unadopted. This closes the adoption gap for tickets, reaction_roles, verification, word_filter, welcome, giveaway, polls, starboard, member_logging (auto_role is owned by 01-05).
Output: 9 modified plugin files + `tests/test_bot_permissions_adoption.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-bug-fixes/01-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01-01)

- New file: `tests/test_bot_permissions_adoption.py`
- New `bot_permissions=[...]` kwargs added to privileged `@slash` decorators across the 9 plugins (no new symbols/classes/functions — decorator-argument changes only)

## Interface context for executors

- `build_slash_callback(..., bot_permissions: list[str] | None = None, ...)` in `easycord/_command_callbacks.py:49-65`; denial branch at lines 179-206 emits `ctx.t("errors.bot_permissions_missing", default="I'm missing the following permission(s): {permissions}.", permissions=...)` and returns before the handler body runs, but only when `ctx.guild` is not None.
- `@slash` / `@slash_command` forward `bot_permissions` from `easycord/decorators.py` into `build_slash_callback`. Mirror `easycord/plugins/moderation.py:127` exactly: `@slash(description="...", guild_only=True, bot_permissions=["kick_members"])`.
- Test harness fact (load-bearing): in `easycord/testing.py`, `self.channel.permissions_for = MagicMock(return_value=guild_permissions)` (line 128) and `guild_permissions` is built from `FakeContextBuilder.with_permissions(**permissions)`. Because `ctx.bot_permissions` resolves to `channel.permissions_for(guild.me)` (`_context_base.py:442-451`), calling `.with_permissions(manage_roles=False)` drives the bot-permission denial path. `_FakePermissions` defaults unknown perms to `False`, so a permission omitted from `with_permissions` reads as absent.

<tasks>

<task type="auto">
  <name>Task 1: Verify the finding — confirm bot_permissions is absent on the 9 target plugins' privileged commands</name>
  <files>easycord/plugins/tickets.py, easycord/plugins/reaction_roles.py, easycord/plugins/verification.py, easycord/plugins/word_filter.py, easycord/plugins/welcome.py, easycord/plugins/giveaway.py, easycord/plugins/polls.py, easycord/plugins/starboard.py, easycord/plugins/member_logging.py</files>
  <read_first>
    - easycord/_command_callbacks.py (lines 49-206: build_slash_callback signature `bot_permissions` param and the denial branch at 179-206)
    - easycord/plugins/moderation.py (lines 120-230: reference adopter)
    - easycord/plugins/tickets.py
    - easycord/plugins/reaction_roles.py
    - easycord/plugins/verification.py
    - easycord/plugins/word_filter.py
    - easycord/plugins/welcome.py
    - easycord/plugins/giveaway.py
    - easycord/plugins/polls.py
    - easycord/plugins/starboard.py
    - easycord/plugins/member_logging.py
  </read_first>
  <action>
    Per the locked cross-cutting rule, verify the finding before changing anything. For each of the 9 plugins, enumerate every `@slash`-decorated command and identify which invoke a privileged Discord API on the bot's behalf: channel/category creation and overwrites for tickets; `member.add_roles`/`member.remove_roles` for reaction_roles and verification; `message.delete`/`purge` for word_filter; channel `.send`/webhook for welcome and member_logging; `message.add_reaction`/`.send` for giveaway/polls/starboard. For each privileged command, confirm no `bot_permissions=[...]` kwarg is present on its decorator. Record the per-command permission mapping to apply, using the CONTEXT.md baseline: tickets->manage_channels; reaction_roles/verification->manage_roles; word_filter->manage_messages; welcome/member_logging->send_messages plus embed_links or manage_webhooks only where the handler actually builds embeds or webhooks; giveaway/polls/starboard->send_messages plus add_reactions only where the handler calls `add_reaction`. Apply Claude's Discretion: derive each command's exact permission list strictly from the Discord API calls that handler actually makes. If any target command ALREADY declares `bot_permissions`, note it and skip modifying it (degrade to test-only coverage for that command).
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && grep -rn "bot_permissions" easycord/plugins/tickets.py easycord/plugins/reaction_roles.py easycord/plugins/verification.py easycord/plugins/word_filter.py easycord/plugins/welcome.py easycord/plugins/giveaway.py easycord/plugins/polls.py easycord/plugins/starboard.py easycord/plugins/member_logging.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - A recorded per-command permission mapping exists covering every privileged command across the 9 plugins, each permission justified by an actual Discord API call in that handler.
    - Any command already declaring `bot_permissions` is listed as pre-existing and excluded from source edits (still covered by a test).
  </acceptance_criteria>
  <done>The set of privileged commands and their target permission lists is fixed; baseline (which commands already have bot_permissions, if any) is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing bot_permissions denial tests, then add bot_permissions to the 9 plugins</name>
  <files>tests/test_bot_permissions_adoption.py, easycord/plugins/tickets.py, easycord/plugins/reaction_roles.py, easycord/plugins/verification.py, easycord/plugins/word_filter.py, easycord/plugins/welcome.py, easycord/plugins/giveaway.py, easycord/plugins/polls.py, easycord/plugins/starboard.py, easycord/plugins/member_logging.py</files>
  <read_first>
    - easycord/testing.py (FakeContextBuilder ~211-322, with_permissions ~269-274, channel.permissions_for wiring ~98-128, invoke helpers ~325-451)
    - easycord/_command_callbacks.py (denial branch 179-206)
    - easycord/plugins/moderation.py (127-230, reference adopter)
    - tests/test_stress.py (TestBugs docstring convention ~49-95)
    - The 9 plugin files listed in files_modified (current state from Task 1)
  </read_first>
  <behavior>
    - For each privileged command in each of the 9 plugins: invoking it in a guild where the bot LACKS a declared permission (built via `FakeContextBuilder().in_guild(...).with_permissions(<declared_perm>=False)`) responds with the `errors.bot_permissions_missing` default text containing "missing the following permission" and the privileged Discord call is NOT reached.
    - Invoking the same command with all declared bot permissions granted (`with_permissions(<declared_perm>=True, ...)` plus any user permission the command's own `permissions=[...]` requires) reaches the handler body (no bot-permission denial).
    - At least one representative command per plugin is exercised (denial + allow), not necessarily every command.
  </behavior>
  <action>
    Create `tests/test_bot_permissions_adoption.py`. Write the denial/allow tests FIRST using `easycord.testing` (`invoke` or `PluginTestSuite`, `FakeContextBuilder`, `with_permissions`) — no ad-hoc Discord mocking. Run them and confirm the denial tests FAIL (because bot_permissions is not yet declared). Then edit each plugin: add the `bot_permissions=[...]` kwarg to each privileged command's `@slash` decorator per the Task 1 mapping, mirroring `moderation.py:127`. Do not change handler bodies. Do not add `bot_permissions` to non-privileged read-only commands. Re-run and confirm green. Localize denial only through the existing `errors.bot_permissions_missing` key (already provided by the mechanism) — do not hardcode response strings. Keep tests deterministic — no wall-clock assertions.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && python -m pytest tests/test_bot_permissions_adoption.py tests/test_tickets.py tests/test_verification.py tests/test_word_filter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_bot_permissions_adoption.py` exists and passes; it asserts the localized denial text ("missing the following permission") for a bot-permission-lacking context on at least one command per plugin.
    - `grep -c "bot_permissions" easycord/plugins/tickets.py` and each of the other 8 target files is >= 1 (each plugin has at least one adopted command; commands already adopted count).
    - `python -m pytest tests/test_tickets.py tests/test_verification.py tests/test_word_filter.py -q` stays green (no regressions in existing plugin suites).
  </acceptance_criteria>
  <done>Every privileged command in the 9 plugins declares an accurate `bot_permissions` list and denies gracefully when the bot lacks a permission, proven by the new regression file with existing suites still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user -> slash command | Untrusted invocation; command may trigger privileged bot actions (role/channel/message mutation) |
| Bot -> Discord API | Bot performs privileged actions gated by its own guild permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | Privileged commands executing partway then raising Forbidden | low | mitigate | Declare `bot_permissions` so the command denies with a localized message before any privileged API call, avoiding partial-effect failures |
| T-01-02 | Elevation of Privilege | Over-broad permission declaration masking missing scoping | low | mitigate | Derive each command's `bot_permissions` strictly from the API calls the handler makes; do not add unused permissions |
</threat_model>

<verification>
Run: `python -m pytest tests/test_bot_permissions_adoption.py tests/test_tickets.py tests/test_verification.py tests/test_word_filter.py -q` — green.
Ownership check: this plan touches only the 9 listed plugin files plus the new test; it must NOT modify auto_role.py (owned by 01-05).
</verification>

<success_criteria>
- All 9 plugins' privileged commands declare accurate `bot_permissions`.
- New regression file proves localized graceful denial when the bot lacks a permission.
- Existing tickets/verification/word_filter suites remain green.
</success_criteria>

<output>
Create `.planning/phases/01-verified-bug-fixes/01-01-SUMMARY.md` when done.
</output>
