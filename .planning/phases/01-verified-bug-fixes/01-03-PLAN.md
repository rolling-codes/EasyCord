---
phase: 01-verified-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - easycord/_command_callbacks.py
  - easycord/bot.py
  - tests/test_cooldown_cleanup.py
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - "Building a slash callback with a cooldown under a running loop emits no DeprecationWarning from asyncio loop acquisition"
    - "Cooldown bucket memory is reclaimed by exactly one sweep mechanism, not two redundant ones"
    - "A cooldown registry cannot grow without bound: once it exceeds a max-entries cap, the oldest buckets are evicted"
  artifacts:
    - tests/test_cooldown_cleanup.py
  key_links:
    - "build_slash_callback loop acquisition (_command_callbacks.py:87) -> asyncio.get_running_loop() -> no DeprecationWarning"
    - "bot._cooldown_registries -> _prune_cooldown_registries (bot.py:433) -> max-entries cap with oldest-bucket eviction"
---

<objective>
Clean up the cooldown sweep machinery (per REQ-03): replace the deprecated `asyncio.get_event_loop()` with `asyncio.get_running_loop()`; remove the redundant per-callback sweep task now that the bot-level `_cooldown_cleanup_loop` covers all registries via `bot._cooldown_registries`; and add a max-entries cap with oldest-bucket eviction to `_prune_cooldown_registries`.

Purpose: `_command_callbacks.py:87` uses the deprecated `asyncio.get_event_loop()` (raises `DeprecationWarning` under `-W error`), the per-callback `_cooldown_sweep_loop` duplicates the bot-level cleanup, and no cap bounds registry growth.
Output: modified `easycord/_command_callbacks.py` + `easycord/bot.py` (this plan exclusively owns both) + extended `tests/test_cooldown_cleanup.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-bug-fixes/01-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 01-03)

- Modified `easycord/_command_callbacks.py`: `asyncio.get_event_loop()` -> `asyncio.get_running_loop()` in try/except (line ~87); redundant per-callback `_cooldown_sweep_loop` scheduling dropped (or deduped if drop proves unsafe)
- Modified `easycord/bot.py`: `_prune_cooldown_registries` gains a max-entries cap constant (default 50_000) with oldest-bucket-first eviction
- New module-level constant in `bot.py` for the cap (e.g. `_COOLDOWN_MAX_ENTRIES`)
- Extended `tests/test_cooldown_cleanup.py`

## Interface context for executors

- `_command_callbacks.py:78-95`: under `if cooldown is not None:` a background `_cooldown_sweep_loop` task is scheduled via `loop = asyncio.get_event_loop()` (line 87, deprecated) then `loop.create_task(...)`. The registry is ALSO appended to `bot._cooldown_registries` at line 73-74, which the bot-level loop consumes.
- `bot.py:105`: `self._cooldown_registries: list[tuple[dict[Any, list[float]], float]] = []`. `bot.py:428-431`: `_cooldown_cleanup_loop` is scheduled on ready and calls `_prune_cooldown_registries(time.monotonic())` (bot.py:433-453) every `cooldown_cleanup_interval`. `_prune_cooldown_registries` iterates each `(cooldown_dict, window)` and drops keys whose timestamps are all older than `window`.
- Each cooldown bucket dict maps `bucket_key -> list[float]` (monotonic timestamps). "Oldest bucket" = the key whose most recent timestamp (`max(timestamps)`) is smallest.

<tasks>

<task type="auto">
  <name>Task 1: Verify the finding — deprecated loop call, redundant sweep, and missing cap</name>
  <files>easycord/_command_callbacks.py, easycord/bot.py</files>
  <read_first>
    - easycord/_command_callbacks.py (lines 14-95: `_cooldown_sweep_loop`, the cooldown block in `build_slash_callback` including the `get_event_loop` call and `bot._cooldown_registries.append`)
    - easycord/bot.py (lines 99-106, 420-453: registry init, cleanup-loop scheduling, `_prune_cooldown_registries`, `_cooldown_cleanup_loop`)
    - tests/test_cooldown_cleanup.py (existing coverage of the cleanup loop and registries)
  </read_first>
  <action>
    Per the locked verify-first rule, confirm each sub-finding before changing anything. Confirm: (a) `_command_callbacks.py:87` calls `asyncio.get_event_loop()` (deprecated); (b) the registry appended at line 73-74 to `bot._cooldown_registries` is the SAME dict the per-callback `_cooldown_sweep_loop` prunes — establishing that the bot-level `_cooldown_cleanup_loop` already covers it, so the per-callback sweep is redundant (apply Claude's Discretion: prefer DROP if coverage is confirmed; otherwise DEDUPE); (c) `_prune_cooldown_registries` has no max-entries cap. If any sub-finding is already fixed, degrade that item to regression-test-only.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && grep -n "get_event_loop\|get_running_loop\|_cooldown_sweep_loop\|_COOLDOWN_MAX" easycord/_command_callbacks.py easycord/bot.py</automated>
  </verify>
  <acceptance_criteria>
    - Recorded: whether line 87 still uses `get_event_loop`.
    - Recorded: proof that `bot._cooldown_registries` covers the per-callback dict (so drop is safe) OR a stated reason to dedupe instead.
    - Recorded: absence of any max-entries cap in `_prune_cooldown_registries`.
  </acceptance_criteria>
  <done>The three sub-fixes are each classified open vs already-fixed; the drop-vs-dedupe decision for the per-callback sweep is made with evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests, then fix loop call + remove redundant sweep + add capped eviction</name>
  <files>tests/test_cooldown_cleanup.py, easycord/_command_callbacks.py, easycord/bot.py</files>
  <read_first>
    - easycord/_command_callbacks.py (cooldown block 49-95)
    - easycord/bot.py (420-453)
    - tests/test_cooldown_cleanup.py (extend, do not rewrite existing tests)
    - tests/test_memory_safety.py (referenced by the verify command; ensure no regression)
  </read_first>
  <behavior>
    - Building a slash callback with a cooldown while an event loop is running produces NO `DeprecationWarning` (test runs under `-W error::DeprecationWarning`, so a deprecation would surface as an error).
    - The per-callback sweep is no longer scheduled: creating a cooldown callback does not add a second sweep task per callback (only the bot-level cleanup loop reclaims memory). Assert on the absence of the per-callback `_cooldown_sweep_loop` task (e.g. it is no longer referenced in the scheduling path, or no extra `_background_tasks` sweep entry is created for a cooldown callback).
    - `_prune_cooldown_registries` enforces a cap: when a registry dict exceeds `_COOLDOWN_MAX_ENTRIES`, pruning evicts oldest buckets (smallest `max(timestamps)` first) until the dict is at or below the cap. Drive this with injected timestamps and a mocked/passed `now` — no wall-clock sleeps.
    - Existing normal-expiry pruning behavior (drop buckets whose timestamps are all older than `window`) still holds.
  </behavior>
  <action>
    Extend `tests/test_cooldown_cleanup.py` with the four behaviors FIRST and confirm they FAIL. Then implement: in `_command_callbacks.py` replace `asyncio.get_event_loop()` at line 87 with `asyncio.get_running_loop()` inside the existing try/except RuntimeError; and remove the redundant per-callback `_cooldown_sweep_loop` scheduling block (drop per the Task 1 decision; if dedupe was chosen instead, document why in the SUMMARY). In `bot.py` add a module-level `_COOLDOWN_MAX_ENTRIES` constant (default 50_000) and extend `_prune_cooldown_registries` so that, after normal expiry pruning, any registry dict still exceeding the cap has its oldest buckets (by `max(timestamps)`) evicted down to the cap; keep the existing broad `except Exception: logger.exception(...)` guard. Re-run under `-W error::DeprecationWarning` and confirm green. Use mocked/passed clocks and injected timestamps only — no real sleeps, no wall-clock assertions.
  </action>
  <verify>
    <automated>cd "/c/Users/Tom/Code projects/EasyCord" && python -m pytest tests/test_cooldown_cleanup.py tests/test_memory_safety.py -q -W error::DeprecationWarning</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cooldown_cleanup.py tests/test_memory_safety.py -q -W error::DeprecationWarning` passes (no DeprecationWarning-as-error surfaces).
    - `grep -c "get_event_loop" easycord/_command_callbacks.py` is 0; `grep -c "get_running_loop" easycord/_command_callbacks.py` is >= 1.
    - `grep -c "_COOLDOWN_MAX_ENTRIES" easycord/bot.py` is >= 1 and a test proves oldest-bucket eviction when the cap is exceeded.
    - A test proves the per-callback sweep is no longer scheduled for a cooldown callback.
  </acceptance_criteria>
  <done>Cooldown scheduling is deprecation-clean, memory is reclaimed by a single mechanism, and registries are hard-capped with oldest-bucket eviction — all proven deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user -> cooldown-gated command | High-cardinality user/guild keys accumulate as cooldown bucket entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Denial of Service | Unbounded cooldown registry growth | medium | mitigate | Add `_COOLDOWN_MAX_ENTRIES` cap with oldest-bucket eviction so a flood of distinct bucket keys cannot exhaust memory; note user-keyed buckets ("user" bucket) are attacker-influenceable, unlike guild-keyed caches |
| T-01-05 | Denial of Service | Redundant per-callback sweep tasks (one per cooldown command) | low | mitigate | Drop the redundant sweep so only the single bot-level cleanup loop runs, reducing background-task pressure |
</threat_model>

<verification>
Run: `python -m pytest tests/test_cooldown_cleanup.py tests/test_memory_safety.py -q -W error::DeprecationWarning` — green.
Ownership check: this plan exclusively owns `easycord/_command_callbacks.py` and `easycord/bot.py`; no other plan may touch them.
</verification>

<success_criteria>
- No DeprecationWarning from cooldown scheduling under `-W error::DeprecationWarning`.
- Exactly one sweep mechanism reclaims cooldown memory.
- Registries capped at `_COOLDOWN_MAX_ENTRIES` with oldest-bucket eviction.
</success_criteria>

<output>
Create `.planning/phases/01-verified-bug-fixes/01-03-SUMMARY.md` when done.
</output>
